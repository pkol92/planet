import React from "react";
import { Center } from "../Styled/component";

const Loading = () => {
	return (
		<div>
			<Center style={{ fontSize: "1.3rem", color: "#f5f5f5" }}>
				Loading...
			</Center>
		</div>
	);
};

export default Loading;
